Basic Syntax and structure of a C Program
Preprocessor directive or command
main function
{
	statements(code)
}

Ex: Hello World Program in C

-------------------------------------------
#include<stdio.h>
void main()
{
	printf("Hello, World!");
}

std - Standard
i - input
o - output
h - header

{} --> Curly or flower braces
<> --> Angle braces
() --> Angle brackets
--------------------------------------------

27-10-2021
--------------------------------------------Code------------------------------------------------------------------
//Finding ranges of int family in compilers

#include<stdio.h>
#include<limits.h> // all limits in this header file
void main()
{
	/*use appropiate format specifiers for every it family 
	  such as for short int --->signed %hd or %hi
	                            unsigned %hu */
	printf("Ranges of Integer Family:\n\n");                           
	//Short int
	printf("Short int:\nsigned: %hd & %hd\n",SHRT_MIN,SHRT_MAX); 
	printf("un signed: %d & %hu\n\n",0,USHRT_MAX);  //for unsigned there is no certain code for upper limit
	
	//int	
	printf("int:\nsigned: %d & %d\n",INT_MIN,INT_MAX);
	printf("un signed: %d & %u\n\n",0,UINT_MAX);
	
	//long in
	printf("long int:\nsigned: %ld & %ld\n",LONG_MIN,LONG_MAX);
	printf("un signed: %d & %lu\n\n",0,ULONG_MAX);
	
	
	//long long int
	/*In some online compilers & another code for 
	Long long int ---> Signed : LLONG_MIN , LLONG_MAX & Unsigned: ULLONG_MAX */
	
	printf("long long int:\nsigned: %lld & %lld\n",LONG_LONG_MIN,LONG_LONG_MAX); 
	printf("un signed: %d & %llu\n\n",0,ULONG_LONG_MAX);
	
}
-------------------------------------------------------------------------------------------------------------------

2-11-2021
Operators in C

Terminology:
Operand(s);
On which the operation is being performed
Operator:
The one which is performing the Operation
Ex:
10+2
Operands??: 10,2
Operator?: +

Arthmetic Operators in C
+ --> Adittion --> Sun
- --> Subtraction --> Difference
* --> Multiplication --> Product
/ --> Division --> Quotient
% --> Modulo division --> Remainder

--------------------------------Code------------------------------------
//All Arthmetic Operations
#include<stdio.h>
void main()
{
	//Variable declaration
	int a, b, sum, difference, product, remainder;
	float quotient;
	
	//initialization
	a=11;
	b=2;
	
	sum = a+b;
	difference = a-b;
	product = a*b;
	quotient = a*1.0/b; //Multiply with 1.0 to convert int to float
	remainder = a%b;
	
	//Output
	printf("Sum of %d and %d is %d:\n", a, b, sum);
	printf("Difference of %d and %d is %d:\n", a, b, difference);
	printf("Product of %d and %d is %d:\n", a, b, product);
	printf("Quotient of %d and %d is %f:\n", a, b, quotient);
	printf("Remainder of %d and %d is %d:\n", a, b, remainder);
}
-------------------------------------------------------------------------

9/11/21
Introduction to scanf statement
Reading the values from user
scanf() statement
syntax:
scanf ("format specifier", &variable)
& - amp

Introduction to ASCII Table
American
Standard
Code for
Information
Interchange


3/5/2022
control statements
entry control loops are while and for
exit control loops are do-while
