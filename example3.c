//C Program to convert to minutes when both hours(s) and minute(s) are given
#include<stdio.h>
void main()
{
	//Variable declaration and intilization
	int h=1,m=40,t=h*60+m;
	
	//output
	printf("%d minutes",t);
}
